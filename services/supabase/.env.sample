# Copy as it is to .env file
SUPABASE_PROJECT_ID=kindfi-project-db

# Resend SMTP for Supabase Auth emails (local `supabase start` only).
# Production: configure the same values in Supabase Dashboard → Authentication → SMTP.
RESEND_SMTP_API_KEY=re_xxxxxxxx